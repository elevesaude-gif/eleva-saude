import { archiveProductAction, restoreProductAction } from "@/app/admin/cms-actions";
import { listAdminProducts } from "@/lib/admin/products";
import { formatCurrency } from "@/lib/currency";
import Link from "next/link";
export default async function ProductsPage(){const products=await listAdminProducts();return <main className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-[#047857]">Catálogo</p><h1 className="font-serif text-4xl font-semibold">Produtos</h1></div><Link className="rounded-xl bg-[#047857] px-4 py-3 text-sm font-bold text-white" href="/admin/products/new">Novo produto</Link></div><div className="mt-7 overflow-x-auto rounded-[24px] border border-[#E6E8ED] bg-white"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-[#0D1B2A] text-white"><tr>{["Ordem","Produto","Categoria","Preço","Estoque","Status",""] .map(x=><th className="p-3" key={x}>{x}</th>)}</tr></thead><tbody>{products.map(product=><tr className="border-t border-[#E6E8ED]" key={product.id}><td className="p-3">{product.sort_order}</td><td className="p-3"><strong>{product.name}</strong><small className="block text-[#344563]">{product.id}</small>{product.updated_at&&<small className="block text-[#344563]">Atualizado: {new Date(product.updated_at).toLocaleString("pt-BR")}</small>}</td><td className="p-3">{product.category}</td><td className="p-3">{formatCurrency(product.price_cents/100)}</td><td className="p-3">{product.stock??"Ilimitado"}</td><td className="p-3">{product.deleted_at?"Arquivado":product.active?"Ativo":"Inativo"}</td><td className="p-3"><div className="flex gap-2"><Link className="rounded-lg border px-3 py-2 font-bold" href={`/admin/products/${product.id}`}>Editar</Link>{product.deleted_at?<form action={restoreProductAction}><input type="hidden" name="id" value={product.id}/><button className="rounded-lg border px-3 py-2 font-bold text-[#047857]">Restaurar inativo</button></form>:<form action={archiveProductAction}><input type="hidden" name="id" value={product.id}/><button className="rounded-lg border border-red-200 px-3 py-2 font-bold text-red-700">Arquivar</button></form>}</div></td></tr>)}</tbody></table></div></main>}
