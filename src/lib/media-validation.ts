export const MAX_MEDIA_BYTES=5*1024*1024;
export function validateMediaBytes(bytes:Uint8Array,type:string,size:number){if(!["image/jpeg","image/png","image/webp"].includes(type))throw new Error("Formato inválido. Use JPG, PNG ou WebP.");if(size<=0||size>MAX_MEDIA_BYTES)throw new Error("A imagem deve ter no máximo 5 MB.");if(!matchesSignature(bytes,type))throw new Error("O conteúdo do arquivo não corresponde ao formato informado.");}
function matchesSignature(bytes:Uint8Array,type:string){if(type==="image/jpeg")return bytes.length>=3&&bytes[0]===0xff&&bytes[1]===0xd8&&bytes[2]===0xff;if(type==="image/png")return bytes.length>=8&&[0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a].every((value,index)=>bytes[index]===value);if(type==="image/webp")return bytes.length>=12&&String.fromCharCode(...bytes.slice(0,4))==="RIFF"&&String.fromCharCode(...bytes.slice(8,12))==="WEBP";return false;}
